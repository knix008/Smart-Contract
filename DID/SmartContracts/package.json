{
  "name": "did-smart-contracts",
  "version": "1.0.0",
  "description": "Decentralized Identity Smart Contracts for Sepolia Testnet",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "verify:sepolia": "hardhat verify --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "ethers": "^6.8.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}